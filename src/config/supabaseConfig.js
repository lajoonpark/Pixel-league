// Supabase project credentials for multiplayer support.
// Get them from: https://app.supabase.com → Settings → API
//
// Fill in your values below and deploy.
// IMPORTANT: Only use the public anon/public key. Never paste your service role key here.
//
// For Vercel: you may also set VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY build-time env vars
// and replace the strings below with import.meta.env.VITE_SUPABASE_URL etc. if you add a
// Vite build step.  For a simple static deploy (no build step), just fill in the strings.

export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';
